{
  "name": "hedoffice",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first agent orchestration with an office-style spatial UX",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "check": "pnpm -r build && pnpm -r typecheck && pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
