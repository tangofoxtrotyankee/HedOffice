# @hedoffice/audio

The audio subsystem: `SttProvider` / `TtsProvider` abstraction (the interface
mandates `cancel()` for barge-in) with a local impl (sherpa-onnx: STT/TTS/VAD)
and a hosted impl (ElevenLabs). Lands in **Phase 2**.

_Placeholder — no code yet. See [docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md)._
