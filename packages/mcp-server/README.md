# @hedoffice/mcp-server

The stateful Streamable HTTP MCP server. Per-session `McpServer` factory keyed
by `Mcp-Session-Id` (one cubicle = one isolated session). Hosts the `notebook.*`,
`task.*`, `channel.*`, and `cubicle.status` tools. Lands in **Phase 1**.

_Placeholder — no code yet. See [docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md)._
